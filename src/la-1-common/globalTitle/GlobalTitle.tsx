import React from 'react';
import s from './GlobalTitle.module.css'

const GlobalTitle = () => {
    return (
        <h1 className={s.globalTitle}>It-incubator</h1>
    );
};

export default GlobalTitle;